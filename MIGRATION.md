# Go → Rust 迁移分析

原项目为 Windows 专用托盘控制器：main.go 管理菜单和进程，download.go 下载更新，autostart.go 写入 HKCU Run，msg_box.go 调用 MessageBoxW，log/ 使用 logrus 和 lumberjack。

| 原实现问题 | Rust 处理 |
| --- | --- |
| 更新线程、进程等待线程共享可变状态 | 主线程独占菜单和进程，下载线程用 channel 返回结果 |
| Kill 后固定等待一秒 | kill + wait，确认退出后替换 |
| 备份后直接解压，失败未恢复 | 先完整解压到同目录临时目录，替换失败恢复备份 |
| 下载写死 amd64 | 根据编译架构精确匹配资源名 |
| 相对工作目录读取配置 | 相对 EXE 所在目录 |
| 只记录 stdout，输出用作格式字符串 | 按字节捕获 stdout/stderr，保留百分号和长行 |
| 自启动路径无引号 | 带引号绝对路径，并识别旧值 |

保留中文菜单、双状态图标、启动时自动启动内核，同时支持手动启停、退出停止内核、记事本日志、自启动键名和更新前运行状态。

调整：日志采用 10 MiB 和单个备份，替代三天保留策略；默认不强制提权，TUN 模式手动以管理员运行；成功启动通过图标反馈，不再弹框。

下载使用 HTTPS 和 HTTP 状态检查，仅提取根目录单一 Mihomo EXE，限制下载/解压大小并校验 ZIP、MZ 头。MZ 检查不等同签名验证。常规退出不会应用后台未完成的更新；强制结束或掉电可能留下临时目录。

Rust 分支已移除 Go 源码、模块依赖、旧 go-winres 配置和 Go 构建产物。迁移前源码（包括迁移开始时的工作区修改）保留在 Git 历史中；当前项目只使用 Cargo 构建。

真实托盘交互、Mihomo/TUN 网络功能、登录自启动需在用户环境验收；测试不会修改注册表或代理状态。

验证：cargo fmt --check、cargo test --locked（5 项测试）、cargo clippy --all-targets --locked -- -D warnings 通过。测试覆盖原始 ICO 解码、架构选择、成功替换保留备份、失败回滚、ZIP 提取与路径穿越/无效 EXE 拒绝。


Release 构建通过。启动冒烟测试：从其他工作目录启动，进程保持运行且无主窗口，随后终止测试进程；未启动 Mihomo 或改动自启动注册表。此检查不替代托盘菜单的手动验收。
